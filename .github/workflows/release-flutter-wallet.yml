name: "Release Flutter Wallet (Testnet)"

# ═══════════════════════════════════════════════════════════════════════════════
# Builds UAT Wallet for macOS, Linux & Windows — creates GitHub Release
# Triggered by testnet tags or manual dispatch
# ═══════════════════════════════════════════════════════════════════════════════

on:
  push:
    tags:
      - 'v*-testnet'
      - 'v*-testnet.*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version (e.g., 1.0.0-testnet)'
        required: true
        default: '1.0.0-testnet'

env:
  FLUTTER_VERSION: '3.27.3'

jobs:
  # ══════════════════════════════════════════════════════════════════════════════
  #  macOS (.dmg)
  # ══════════════════════════════════════════════════════════════════════════════
  build-macos:
    name: "🍎 macOS (.dmg)"
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            flutter_wallet/native/uat_crypto_ffi/target
          key: macos-rust-${{ hashFiles('flutter_wallet/native/uat_crypto_ffi/Cargo.lock') }}

      - name: Build Dilithium5 native library
        working-directory: flutter_wallet/native/uat_crypto_ffi
        run: |
          cargo build --release
          cargo test --release -- --nocapture

      - name: Flutter pub get
        working-directory: flutter_wallet
        run: flutter pub get

      - name: Flutter build macOS
        working-directory: flutter_wallet
        run: flutter build macos --release

      - name: Bundle native library
        working-directory: flutter_wallet
        run: |
          APP="build/macos/Build/Products/Release/UAT Wallet.app"
          FRAMEWORKS="$APP/Contents/Frameworks"
          mkdir -p "$FRAMEWORKS"
          cp native/uat_crypto_ffi/target/release/libuat_crypto_ffi.dylib "$FRAMEWORKS/"
          install_name_tool -id "@executable_path/../Frameworks/libuat_crypto_ffi.dylib" "$FRAMEWORKS/libuat_crypto_ffi.dylib"
          codesign --force --deep --sign - "$APP"

      - name: Create DMG
        working-directory: flutter_wallet
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          [ -z "$VERSION" ] && VERSION="${{ github.event.inputs.version }}"
          
          mkdir -p release/dmg_staging
          cp -R "build/macos/Build/Products/Release/UAT Wallet.app" release/dmg_staging/
          ln -s /Applications release/dmg_staging/Applications
          
          cat > release/dmg_staging/README.txt << 'EOF'
          ╔══════════════════════════════════════════════════════════════╗
          ║            UAT WALLET - TESTNET RELEASE                    ║
          ║            Unauthority Blockchain                          ║
          ╚══════════════════════════════════════════════════════════════╝
          
          INSTALLATION:
            Drag "UAT Wallet.app" to the Applications folder.
          
          FIRST RUN:
            1. Open the app from Applications
            2. If blocked: System Settings → Privacy → Open Anyway
            3. Wallet auto-configures Tor + Dilithium5 crypto
          EOF
          
          hdiutil create \
            -volname "UAT Wallet" \
            -srcfolder release/dmg_staging \
            -ov -format UDZO \
            "release/UAT-Wallet-${VERSION}-macos.dmg"
          
          rm -rf release/dmg_staging

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: wallet-macos
          path: flutter_wallet/release/*.dmg
          if-no-files-found: error

  # ══════════════════════════════════════════════════════════════════════════════
  #  Linux (.tar.gz)
  # ══════════════════════════════════════════════════════════════════════════════
  build-linux:
    name: "🐧 Linux (.tar.gz)"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            clang cmake git ninja-build pkg-config \
            libgtk-3-dev liblzma-dev libstdc++-12-dev \
            libsecret-1-dev libjsoncpp-dev

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            flutter_wallet/native/uat_crypto_ffi/target
          key: linux-rust-${{ hashFiles('flutter_wallet/native/uat_crypto_ffi/Cargo.lock') }}

      - name: Build Dilithium5 native library
        working-directory: flutter_wallet/native/uat_crypto_ffi
        run: |
          cargo build --release
          cargo test --release -- --nocapture

      - name: Flutter pub get
        working-directory: flutter_wallet
        run: flutter pub get

      - name: Flutter build Linux
        working-directory: flutter_wallet
        run: flutter build linux --release

      - name: Bundle native library & package
        working-directory: flutter_wallet
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          [ -z "$VERSION" ] && VERSION="${{ github.event.inputs.version }}"
          
          BUNDLE="build/linux/x64/release/bundle"
          
          # Copy .so into bundle/lib/
          cp native/uat_crypto_ffi/target/release/libuat_crypto_ffi.so "$BUNDLE/lib/"
          
          # Create launcher script
          cat > "$BUNDLE/run.sh" << 'LAUNCHER'
          #!/bin/bash
          SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
          export LD_LIBRARY_PATH="${SCRIPT_DIR}/lib:${LD_LIBRARY_PATH}"
          exec "${SCRIPT_DIR}/flutter_wallet" "$@"
          LAUNCHER
          chmod +x "$BUNDLE/run.sh"
          
          # Create README
          cat > "$BUNDLE/README.txt" << 'EOF'
          ═══════════════════════════════════════════════════════════════
            UAT WALLET - TESTNET RELEASE (Linux)
            Unauthority Blockchain
          ═══════════════════════════════════════════════════════════════
          
          INSTALLATION:
            1. Extract this archive
            2. Run: ./run.sh
          
          The wallet automatically handles Tor and crypto setup.
          EOF
          
          # Package as tar.gz
          mkdir -p release
          cd build/linux/x64/release
          tar czf "../../../../release/UAT-Wallet-${VERSION}-linux-x64.tar.gz" bundle/
          cd ../../../../

      - name: Upload Linux artifact
        uses: actions/upload-artifact@v4
        with:
          name: wallet-linux
          path: flutter_wallet/release/*.tar.gz
          if-no-files-found: error

  # ══════════════════════════════════════════════════════════════════════════════
  #  Windows (.zip)
  # ══════════════════════════════════════════════════════════════════════════════
  build-windows:
    name: "🪟 Windows (.zip)"
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            flutter_wallet/native/uat_crypto_ffi/target
          key: windows-rust-${{ hashFiles('flutter_wallet/native/uat_crypto_ffi/Cargo.lock') }}

      - name: Build Dilithium5 native library
        working-directory: flutter_wallet/native/uat_crypto_ffi
        run: |
          cargo build --release
          cargo test --release -- --nocapture

      - name: Flutter pub get
        working-directory: flutter_wallet
        run: flutter pub get

      - name: Flutter build Windows
        working-directory: flutter_wallet
        run: flutter build windows --release

      - name: Bundle native library & package
        working-directory: flutter_wallet
        shell: pwsh
        run: |
          $version = "$env:GITHUB_REF_NAME" -replace '^v',''
          if (-not $version) { $version = "${{ github.event.inputs.version }}" }
          
          $bundle = "build\windows\x64\runner\Release"
          
          # Copy .dll next to the executable
          Copy-Item "native\uat_crypto_ffi\target\release\uat_crypto_ffi.dll" "$bundle\"
          
          # Create README
          @"
          ═══════════════════════════════════════════════════════════════
            UAT WALLET - TESTNET RELEASE (Windows)
            Unauthority Blockchain
          ═══════════════════════════════════════════════════════════════
          
          INSTALLATION:
            1. Extract this archive
            2. Run: flutter_wallet.exe
          
          The wallet automatically handles Tor and crypto setup.
          "@ | Out-File -FilePath "$bundle\README.txt" -Encoding utf8
          
          # Package as zip
          New-Item -ItemType Directory -Force -Path release | Out-Null
          Compress-Archive -Path "$bundle\*" -DestinationPath "release\UAT-Wallet-${version}-windows-x64.zip" -Force

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: wallet-windows
          path: flutter_wallet/release/*.zip
          if-no-files-found: error

  # ══════════════════════════════════════════════════════════════════════════════
  #  Create GitHub Release
  # ══════════════════════════════════════════════════════════════════════════════
  release:
    name: "📦 Create Release"
    needs: [build-macos, build-linux, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get version info
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
            TAG="v${VERSION}"
          else
            TAG="${GITHUB_REF_NAME}"
            VERSION="${TAG#v}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "Version: $VERSION, Tag: $TAG"

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: List artifacts
        run: find artifacts -type f | sort

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: "UAT Wallet ${{ steps.version.outputs.version }} (Testnet)"
          draft: false
          prerelease: true
          generate_release_notes: true
          body: |
            # 🔐 UAT Wallet — Testnet Release `${{ steps.version.outputs.version }}`

            **Post-Quantum Secure** wallet for the Unauthority blockchain testnet.

            ## 📥 Downloads

            | Platform | File | Install |
            |----------|------|---------|
            | 🍎 **macOS** | `UAT-Wallet-*-macos.dmg` | Open DMG → drag to Applications |
            | 🐧 **Linux** | `UAT-Wallet-*-linux-x64.tar.gz` | Extract → `./run.sh` |
            | 🪟 **Windows** | `UAT-Wallet-*-windows-x64.zip` | Extract → `flutter_wallet.exe` |

            ## ✅ What's Included

            - 🔐 **CRYSTALS-Dilithium5** — Post-quantum digital signatures (compiled native library)
            - 🧅 **Automatic Tor** — Zero-config .onion connectivity (auto-installs if needed)
            - 💰 **Send/Receive UAT** — Full transaction support
            - 🔥 **Proof-of-Burn** — Convert BTC/ETH to UAT
            - 📝 **BIP39 Seed Phrase** — 24-word backup & recovery
            - 🛡️ **Anti-Whale Protection** — Dynamic fee scaling, quadratic voting
            - ⚡ **Guaranteed Mainnet** — All features active (Dilithium5, slashing, anti-whale)

            ## 🚀 First Run

            1. Install & open the wallet
            2. The app auto-configures:
               - Downloads/connects Tor (no manual install)
               - Generates quantum-secure Dilithium5 wallet
               - Connects to testnet via `.onion`
            3. **macOS users:** If Gatekeeper blocks → right-click → Open

            ## ⚠️ Notes

            - This is a **testnet** release — tokens have no real value
            - All traffic routed through **Tor** automatically
            - Wallet keys are **quantum-resistant** (CRYSTALS-Dilithium5)

          files: |
            artifacts/wallet-macos/*.dmg
            artifacts/wallet-linux/*.tar.gz
            artifacts/wallet-windows/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
